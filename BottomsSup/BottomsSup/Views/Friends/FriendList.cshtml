@model IEnumerable<BottomsSup.Models.Friends>

@{
    ViewBag.Title = "FriendList";
}

<h2>FriendList</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Client.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Client.FirstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Clients.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Clients.FirstName)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.FriendId }) |
            @Html.ActionLink("Details", "Details", new { id=item.FriendId }) |*@
            @Html.ActionLink("Delete", "Delete", new { id=item.FriendId })
        </td>
    </tr>
}

</table>
